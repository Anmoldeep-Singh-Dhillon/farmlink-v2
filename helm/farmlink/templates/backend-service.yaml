apiVersion: v1
kind: Service
metadata:
  name: farmlink-backend-service
spec:
  selector:
    app: farmlink-backend
  ports:
  - port: {{ .Values.backend.port}}
    targetPort: {{ .Values.backend.port}}
  type: ClusterIP