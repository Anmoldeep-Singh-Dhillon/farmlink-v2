apiVersion: v1
kind: Service
metadata:
  name: farmlist-backend-service
spec:
  selector:
    app: farmlist-backend
  ports:
  - port: {{ .Values.backend.port}}
    targetPort: {{ .Values.backend.port}}
  type: ClusterIP